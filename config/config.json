{
  "development": {
    "username": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "database": "${DB_NAME}",
    "host": "${RDS_ENDPOINT}",
    "dialect": "mysql"
  },
  "test": {
    "username": "${DB_USER}",
    "password": null,
    "database": "${DB_NAME_TEST}",
    "host": "${RDS_ENDPOINT}",
    "dialect": "mysql"
  },
  "production": {
    "username": "${DB_USER}",
    "password": null,
    "database": "${DB_NAME_PRODUCTION}",
    "host": "${RDS_ENDPOINT}",
    "dialect": "mysql"
  }
}
